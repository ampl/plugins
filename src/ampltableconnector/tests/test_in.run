reset;

set keys;
param string_values{keys} symbolic;
param numeric_values{keys};

table mytable IN "examplehandler":
	keys <- [keys], string_values, numeric_values;

load examplehandler.dll;

# this will search for the file "mytable.hxt" (which is empty)
# and load the data hard coded in the function read_in() 
read table mytable;

display keys;
display string_values;
display numeric_values;
